import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.glowingslothbounce',
  appName: 'glowing-sloth-bounce',
  webDir: 'dist'
};

export default config;
